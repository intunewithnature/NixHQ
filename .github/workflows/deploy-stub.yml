name: Deploy (stub)

on:
  workflow_dispatch:

jobs:
  apply:
    if: ${{ false }} # Placeholder until deploy secrets land
    runs-on: ubuntu-latest
    steps:
      - name: Explain how to enable
        run: |
          echo "This workflow is intentionally disabled."
          echo "Set NIX_SSH_PRIVATE_KEY and target host secrets, then flip the job condition."

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Nix
        uses: cachix/install-nix-action@v27
        with:
          nix_path: nixpkgs=github:NixOS/nixpkgs/4c8cdd5b1a630e8f72c9dd9bf582b1afb3127d2c

      - name: Placeholder deploy commands
        run: |
          echo "Example:"
          echo "  nix build .#nixosConfigurations.test-server.config.system.build.toplevel"
          echo "  nix copy --to ssh://root@test-server /nix/store/<closure>"
          echo "  ssh root@test-server sudo nix-env -p /nix/var/nix/profiles/system --set /nix/store/<closure>"
          echo ""
          echo "Wire these commands up once secrets exist."
